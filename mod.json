{
	"geode": "4.10.0",
	"gd": {
		"win": "2.2074",
		"android": "2.2074",
		"mac": "2.2074",
		"ios": "2.2074"
	},
	"id": "sweep.auto-rater",
	"name": "Auto Rater",
	"version": "v1.2.0",
	"developer": "Sweep",
	"description": "Automatically rates the stars of levels.",
	"links": {
		"source": "https://github.com/SweepSweep2/gd-auto-rater"
	},
	"issues": {
		"info": "You can report issues by either creating a new issue on the GitHub, or messaging me on Discord: @sweepsweep2",
		"url": "https://github.com/SweepSweep2/gd-auto-rater/issues"
	},
	"incompatibilities": {
		"hbg1010.autorate": {
			"version": ">=1.0.0",
			"importance": "breaking"
		}
	},
	"settings": {
		"enable-auto-rater": {
			"type": "bool",
			"name": "Enable Auto-Rater",
			"default": true
		},
		"separator": {
			"type": "title",
			"name": "Rate Settings"
		},
		"rate-mode-selection": {
			"type": "string",
			"name": "Rate Mode",
			"default": "Requested Difficulty",
			"one-of": ["Requested Difficulty", "Current Difficulty", "Override"],
			"enable-if": "setting:enable-auto-rater"
		},
		"override-difficulty-rate": {
			"type": "int",
			"name": "Override Rate Difficulty",
			"description": "If the rate mode is set to Override, the mod rates all levels with this difficulty (in stars).",
			"min": 1,
			"max": 10,
			"default": 5,
			"enable-if": "setting:enable-auto-rater",
			"control": {
				"slider": false
			}
		},
		"na-override": {
			"type": "string",
			"name": "NA Override",
			"description": "If the rate mode is set to Requested Difficulty or Current Difficulty and the one selected is NA, then it will rate the level in the way this setting specifies. (if the final difficulty is still NA, it will rate the level as if the Override mode is selected)",
			"default": "Requested Difficulty",
			"one-of": ["Requested Difficulty", "Current Difficulty", "Override"],
			"enable-if": "setting:enable-auto-rater"
		},
		"demon-separator": {
			"type": "title",
			"name": "Demon Rate Settings"
		},
		"override-demon-rate": {
			"type": "bool",
			"name": "Override Demon Rate",
			"description": "Toggles whether the mod should rate using the demon difficulty you set in Override Demon Difficulty Rate.",
			"default": false,
			"enable-if": "setting:enable-auto-rater"
		},
		"override-difficulty-demon-rate": {
			"type": "string",
			"name": "Override Demon Difficulty Rate",
			"description": "If Override Demon Rate is on, the mod will rate all demons with this difficulty.",
			"one-of": ["Easy Demon", "Medium Demon", "Hard Demon", "Insane Demon", "Extreme Demon"],
			"default": "Hard Demon",
			"enable-if": "setting:enable-auto-rater"
		}
	},
	"tags": [
		"online",
		"universal",
		"enhancement"
	]
}
